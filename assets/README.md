# assets

Put your images here (project screenshots, icons, etc.).

Reference them from `projects.js` using a path like `assets/currencyflow.png`.

Recommended: roughly 16:10 ratio (for example 800 x 500 px) so the
project cards look consistent. PNG or JPG both work.
